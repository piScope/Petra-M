[project]
name = "petram"
license = "GPL-3.0-or-later"
description='Petra-M finite element analysis framework base package'
authors = [ { name = "S. Shiraiwa" } ]
maintainers = [ { name = "S. Shiraiwa", email = "shiraiwa@princeton.edu" } ]
keywords = ["scientific computing", "python visualzation", "finite element method", "MFEM"]
readme = "README.md"
requires-python = ">=3.10"
version = "26.7.15"
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "Topic :: Scientific/Engineering :: Physics",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
]
dependencies = [
    "packaging",
    "mfem >= 4.9",
]

[project.optional-dependencies]
"gui" = ["piScope >= 25.5.24",]

[project.urls]
"Homepage" = "https://github.com/piScope/Petra-M"
"Download" = "https://github.com/piScope/Petra-M"

[build-system]
requires = [
        "setuptools>=80.0.1",
        "pip>=25.1.0", 
]

[tool.setuptools]
package-dir = {"" = "python"}

[tool.setuptools.package-data]
"petram" =  ['data/icon/*.png', 'data/*Ops']
